// next-intl.config.ts
import type { IntlConfig } from "next-intl";

const nextIntlConfig: IntlConfig = {
  // Define the default locale here
  locale: "en",
  // Path to your messages folder
  // relative to the root of your project
};

export default nextIntlConfig;
